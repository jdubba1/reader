# Reader

Keep capture local, bind the daemon to loopback, and keep X and Instagram in separate databases. Never commit captured posts, exports, credentials, or generated databases.

Use Python's standard library and plain browser JavaScript. Run `./run-tests.sh` after changes. Tests must use synthetic fixtures and temporary databases.
